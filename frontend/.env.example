# 前端环境变量示例（复制为 frontend/.env.development.local 或生产环境变量使用）

# API 根地址（最终请求为：${VITE_API_BASE_URL}/api/v1/...）
# 生产环境可留空并通过同源反向代理提供 /api/v1；如设置该变量，不能使用 localhost 或示例占位域名。
VITE_API_BASE_URL=http://localhost:8000
